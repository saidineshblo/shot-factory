{
  "mcpServers": {
      "replicate": {
      "command": "node",
      "args": ["${workspaceFolder}/replicate-mcp-fork/index.js"],
      "env": {
        "REPLICATE_API_TOKEN": "${REPLICATE_API_TOKEN}"
      }
    }
  }
}
