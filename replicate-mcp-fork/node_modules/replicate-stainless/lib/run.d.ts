import { Replicate } from "../client.js";
import { Prediction, PredictionCreateParams } from "../resources.js";
export type RunOptions = {
    wait?: {
        mode: 'block' | 'poll';
        timeout?: number;
        interval?: number;
    } | false;
    useFileOutput?: boolean;
    /**
     * Strategy for handling file inputs (File, Blob, Buffer objects):
     * - 'default': Upload files to get URLs, fall back to base64 on server errors
     * - 'upload': Always upload files to get URLs
     * - 'data-uri': Always encode files as base64 data URIs
     */
    fileEncodingStrategy?: 'data-uri' | 'upload' | 'default';
    signal?: AbortSignal;
} & PredictionCreateParams;
export declare function run(client: Replicate, fetch: typeof globalThis.fetch, ref: string, options: RunOptions, progress?: (prediction: Prediction) => void): Promise<any>;
//# sourceMappingURL=run.d.ts.map